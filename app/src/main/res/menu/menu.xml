<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/file"
        android:title="Menu"
        app:showAsAction="always">
        <!-- android:icon="@drawable/ic_baseline_menu_24" -->

        <!-- "file" submenu -->
        <menu>
            <item
                android:id="@+id/obras"
                android:title="" />
            <item
                android:id="@+id/acidente"
                android:title="@string/acidente" />
            <item
                android:id="@+id/terreno"
                android:title="@string/terreno" />
            <item
                android:id="@+id/outro"
                android:title="@string/outro" />
        </menu>
    </item>


    <item android:id="@+id/notes"
        android:title="@string/notas"
        app:showAsAction="always">
    </item>
    <item android:id="@+id/logout"
        android:title="@string/logout"
        app:showAsAction="always"/>





</menu>